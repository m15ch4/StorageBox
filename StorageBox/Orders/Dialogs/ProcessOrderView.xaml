<Window x:Class="StorageBox.Orders.Dialogs.ProcessOrderView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StorageBox.Orders.Dialogs"
        mc:Ignorable="d"
        Title="ProcessOrderView" Height="500" Width="700"
        AllowsTransparency="True" WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False" Background="Transparent">
    <Grid>
        <Border Margin="0,0,8,8" Background="White" BorderThickness="1" BorderBrush="Black" CornerRadius="5">
            <Border.Effect>
                <DropShadowEffect BlurRadius="5" Opacity="0.4"/>
            </Border.Effect>
            <DockPanel>
                <Label DockPanel.Dock="Top" HorizontalAlignment="Center" FontSize="16" Margin="20" FontWeight="Bold">Przetwarzanie zamówienia</Label>
                <Button Name="CloseDialog" Content="Zamknij" DockPanel.Dock="Bottom" Margin="20" HorizontalAlignment="Center" FontSize="16" Width="100"/>
                <ListBox DockPanel.Dock="Top" Name="SBTasks" DisplayMemberPath="ProductVariant.ProductVariantDescriptionNoNumber" Margin="50 0" FontSize="16"/>
            </DockPanel>
        </Border>
    </Grid>
</Window>
